# AI SDK Tools - Dev Container Environment
# Copy this to .env.local and add your actual API keys

# Required - OpenAI API Key
OPENAI_API_KEY=sk-your-key-here

# Optional - Use local Redis in dev container (already configured)
# UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN not needed
# The app will use in-memory storage if Upstash is not configured


